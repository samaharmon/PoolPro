# Playwright authentication — copy this to .env and fill in your credentials.
# .env is gitignored and never committed.

# Supervisor Firebase account (the account you use to log into PoolPro)
POOLPRO_EMAIL=you@example.com
POOLPRO_PASSWORD=yourpassword
